= form_for @client do |f|
  - if @client.errors.any?
    #error_explanation
      %h2= "#{pluralize(@client.errors.count, "error")} prohibited this city from being saved:"
      %ul
        - @client.errors.full_messages.each do |msg|
          %li= msg

  .control-group
    .controls
      = f.text_field :name, :class => 'span8', :placeholder => 'Наименование компании'
  .control-group
    .controls
      = f.text_field :inn, :class => 'span8', :placeholder => 'ИНН компании'
  .control-group
    .controls
      = f.text_field :code, :class => 'span8', :placeholder => 'Код компании'
  .control-group
    .controls
      = f.collection_select :city_id, City.all, :id, :name, {:prompt => 'Выберите город'}, :class => 'span8'
  %hr.separator
  .form-actions
    = f.submit 'Сохранить', :class => 'btn btn-icon btn-primary'

%ul.breadcrumb
  %li Вы сейчас здесь
  %li
    = link_to root_path, :class => 'glyphicons dashboard' do
      %i
      Главная
  %li.divider
  %li= link_to 'Справочник "Филиалы"', branches_path
  %li.divider
  %li= @branch.name
%h3.separator.bottom
  = @branch.name
  %span карточка
.widget.widget-heading-simple.widget-body-gray
  .widget-body
    .menubar.links.primary
      %ul
        %li= link_to 'Редактировать', edit_branch_path(@branch)
    %dl
      %dt Наименование
      %dd= @branch.name
      %dt Руководитель
      %dd= @branch.employee.initials if @branch.employee
      %dt Город
      %dd= @branch.city.name
      %dt Автор
      %dd= @branch.user.username
      %dt Дата создания
      %dd= @branch.created_at.strftime('%d.%m.%Y %H:%M')

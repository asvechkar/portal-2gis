- flash.each do |name, msg|
  %div{ :class => "alert alert-#{name}" }
    %button.close{'data-dismiss' => 'alert', :type => 'button'} &times;
    = msg
%ul.breadcrumb
  %li Вы сейчас здесь
  %li
    = link_to root_path, :class => 'glyphicons dashboard' do
      %i
      Главная
  %li.divider
  %li= link_to 'Справочник "Уровни опыта"', levels_path
  %li.divider
  %li= @level.name
%h3.separator.bottom
  = @level.name
  %span карточка
.widget.widget-heading-simple.widget-body-gray
  .widget-body
    .menubar.links.primary
      %ul
        %li= link_to 'Редактировать', edit_level_path(@level)
    %dl
      %dt Наименование
      %dd= @level.name
      %dt Автор
      %dd= @level.user.username
      %dt Дата создания
      %dd= @level.created_at.strftime('%d.%m.%Y %H:%M')
